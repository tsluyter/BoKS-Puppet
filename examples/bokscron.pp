#
# The class boks::bokscron has one optional parameters, illustrated
# below. The default is to enable the bokscron, but you can also 
# disable it
#
#class { boks::bokscron : bokscron_set_to => 'off', }

class { boks::bokscron } 

